Cada usuario dispone de un espacio de disco limitado (hay un número máximo de ficheros que se puede tener y un límite de capacidad). Para comprobar del que se dispone se emplea el comando quota:
```
$ quota -v
```
